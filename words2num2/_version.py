# Static version file. maturin (unlike setuptools_scm) does not regenerate
# this; the release CI sets pyproject's version from the git tag, and this is
# bumped alongside it so `words2num2.__version__` matches the wheel.
__version__ = "0.3.0"
__version_tuple__ = (0, 3, 0)
