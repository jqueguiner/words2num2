[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "words2num2-rust"
requires-python = ">=3.8"

[tool.maturin]
module-name = "words2num2._rust"
features = ["pyo3/extension-module"]
